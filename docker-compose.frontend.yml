version: '3.8'

services:
  signforce-web:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    container_name: signforce-web
    ports:
      - "8080:80"
    restart: unless-stopped
    labels:
      - "app=signforce"
